{
  "name": "jukebox-pro",
  "type": "module",
  "imports": {
    "#app": "./app.js",
    "#api/*": "./api/*",
    "#db/*": "./db/*",
    "#middleware/*": "./middleware/*",
    "#utils/*": "./utils/*"
  },
  "scripts": {
    "start": "node --env-file=.env server",
    "dev": "node --watch --env-file=.env server",
    "test": "vitest",
    "db:schema": "psql -d jukebox_pro -f db/schema.sql",
    "db:seed": "node --env-file=.env db/seed",
    "db:reset": "npm run db:schema && npm run db:seed"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.1",
    "pg": "^8.14.1"
  },
  "devDependencies": {
    "supertest": "^7.1.0",
    "vitest": "^3.1.1"
  },
  "engines": {
    "node": ">=22"
  }
}
